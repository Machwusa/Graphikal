query GetUserQuery($userId: String!){
    user(id: $userId){
        name
        age
        profession
        posts{
            comment
            id
        }
        hobbies{
            id
            title
            description
        }
    }
}